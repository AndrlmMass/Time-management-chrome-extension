document.addEventListener('DOMContentLoaded', function() {
  // Get the popup's main container element
  const popupContainer = document.getElementById('popup-container');
  
  // Set custom width and height
  popupContainer.style.width = '100px';    // Adjust width as needed
  popupContainer.style.height = '100px';   // Adjust height as needed
});
